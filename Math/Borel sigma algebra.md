
The set this algebtra is defoned on os an **open set**, to be specific - a [[topology]]
$$B(\sigma) = \sigma(\tau)$$
math script: $$\mathcal{B} = \mathcal{B} (X) := \sigma (\mathcal{O})$$

for a given [[topological space]] $(X, \mathcal{O})$, algebra $\mathcal{A} := \{X/V :V \in \mathcal{O}\}$ being the [[closed set]] of X (*because topology per definition has only open sets*), $\mathcal{K} : = \{ K \subseteq X: K \text{ kompakt }\}$ being the set of [[compact]] set 
1. $\mathcal{B} (X) = \sigma ( \mathcal{A})$
2. for a [[countable]] [[Basis]] of the [[topology]]: $$\mathcal{B}(X) = \sigma (\mathcal{E})$$
3. if X is [[hausdorff]]ish and there is a [[sequence]] of [[compact]] sets with $X = \bigcup_{n \in \mathbb{N}} K_n$, then $$\mathcal{B}(X) = \sigma (\mathcal{K})$$

# Remember 
smalles possible algebra for $\mathbb{\hat R} = \{\{- \infty \},\{\infty \}, \{ \text{all open sets of R}\} \}$: $$\mathcal{B} (\mathbb{\hat R}) = \{ \mathcal{B} \subseteq \mathbb{\hat R}: \mathbb{R} \cap B \in \mathcal{B} (\mathbb{R}) \} = \{ B \cup E: B \in \mathcal{R}, E \subseteq \{ - \infty, \infty \} \}$$

smalles possible algebra for $[0, \infty]$: $$\mathcal{B} ([o, \infty]) = \{ B \subseteq [0,\infty ]: B \cap [0, \infty[ \in \mathcal{B} ([0, \infty [) \}$$
so that we have the [[generator]] for $\mathcal{B}(\mathbb{\hat R})$ are $$\mathcal{J} := \{ [\alpha, \infty]: \alpha \in \mathbb{R}$$$$\mathcal{J}_{\mathbb{Q}} = \{ [ \alpha, \infty]: \alpha \in \mathbb{Q}\}$$


==all Borel sets are [[Lebesque sigma algebra|lebesque]] measurable== 