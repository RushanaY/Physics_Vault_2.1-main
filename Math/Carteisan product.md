$$M \times N : = \{ z| \exists x \in M, y \in N: z = (x,y)\}$$ because there is a pair in a tuple, we can use the [[schema of replacement axiom]] to make sure, that there is always something in the set and we write it as :$$M \times N = \{(x,y)| x \in M, y \in N\}$$

also further :$$M^n : = M \times M \times ... \times M$$
	$n$- times 



Because elements of the carteisan products are [[tupel]]s we have the truthful expression $$(m_1, m_2, ..., m_n) \in M_1 \times M_2 \times ... \times M_n \Leftrightarrow m_1 \in M_1 \land m_2 \in M_2 \land ... \land m_n \in M_n$$
we abuse the fact tupels are very keen on the order, which allows to know exacly from which set which element is