4
$$Q_n = \frac{\omega_n}{2 \gamma_n}$$
where $\omega_n$ is the [[eigenfrequency]] $$\omega_n = c \sqrt{\lambda_n} = c \frac{2 \pi}{L}n$$
with $L$ being the length of cavity -> but here we assume a square shaped cavity, not cylindrical 

